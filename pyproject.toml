[project]
name = "sya-detector"
version = "0.1.0"
description = "Detect sycophantic agreement (SYA) in LLM conversations and strip hollow openers (SYPR)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.6",
    "httpx==0.28.1",
    "uvicorn==0.34.0",
]
